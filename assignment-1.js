let numbers = [];

for (let i = 0; i < 10; i++) {
  numbers.push(i);
}

//logs the first item of the array
console.log(numbers[0])

//logs the last item of the array: numbers[9]
console.log(numbers[numbers.length - 1])

let car = {};

car.colour = "black";